name: kakao-local
emoji: "\U0001F4CD"
description: "Kakao Local API via kakao-cli (address, coordinate, keyword search)"
type: cli
status: inactive
required_commands:
  - kakao-cli
install_hints:
  kakao-cli: "Install via pip: pip install git+https://github.com/heg-wtf/kakao-cli.git"
environment_variables:
  - KAKAO_REST_API_KEY
allowed_tools:
  - "Bash(kakao-cli:*)"
  - "Bash(kakao:*)"
