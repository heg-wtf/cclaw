name: daiso
emoji: "🏪"
description: "Daiso Mall product search via daiso-cli"
type: cli
status: inactive
required_commands:
  - daiso
install_hints:
  daiso: "Install via pip: pip install git+https://github.com/heg-wtf/daiso-cli.git"
allowed_tools:
  - "Bash(daiso:*)"
